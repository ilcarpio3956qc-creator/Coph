
  # BombSight Simulator Design (Copy)

  This is a code bundle for BombSight Simulator Design (Copy). The original project is available at https://www.figma.com/design/mdff5gu3rciRiPhmEuWrb9/BombSight-Simulator-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  